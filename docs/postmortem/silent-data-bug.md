# Silent Data Bug

Draft placeholder.

This document will describe the notebook fallback that silently loaded the
default winner corpus instead of the intended mix dataset, and the guardrails
added afterward: hardcoded path, exact example count assertion, and no fallback.
